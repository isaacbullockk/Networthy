# Always use the public npm registry.
# This sandbox resolves through an internal mirror, which leaks into
# package-lock.json "resolved" URLs — unreachable from Railway builders.
registry=https://registry.npmjs.org/
